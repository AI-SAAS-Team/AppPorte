# Le frontend appelle le backend via le proxy interne de Next (/api -> backend).
# Garder /api permet d'exposer toute l'app via un seul tunnel/déploiement.
# (Le proxy cible BACKEND_URL côté serveur, voir next.config.mjs.)
NEXT_PUBLIC_API_URL=/api
